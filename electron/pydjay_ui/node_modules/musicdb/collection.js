// const { EventDispatcher } = require("event_dispatcher")
const { Datastore } = require("nedb-async-await")


class CollectionController extends Datastore {
    constructor(filename) {
        super({
            filename: filename, 
            autoload: true,
            timestampData: true})
    }
}

module.exports = {
    CollectionController: CollectionController
}