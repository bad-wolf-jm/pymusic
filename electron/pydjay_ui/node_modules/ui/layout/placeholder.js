const { DOMElement } = require("ui/dom/domelement.js")


class LayoutPlaceholder extends DOMElement {
    constructor(text) {
        super("div")

        this.setStyle({
            height: "100%",
            maxHeight: "100%"
        })
    }
}

module.exports = {
    LayoutPlaceholder: LayoutPlaceholder
}