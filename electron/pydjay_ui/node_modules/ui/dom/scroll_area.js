
const { DOMElement } = require("ui/dom/domelement.js")


class ScrollArea extends DOMElement {
    constructor() {
        super("div")
        this.setStyle({
            overflow: "auto",
            height: "100%",
            maxHeight: "100%"
        })
    }
}

module.exports = {
    ScrollArea: ScrollArea
}