const { DOMElement } = require("ui/dom/domelement.js")


class TextInput extends DOMElement {
    constructor() {
        super("input")
        this.domElement.type = "text"
        this.domElement.classList.add('main-track-search')
    }

    setText(text) {
        this.domElement.innerHTML = text
    }
}

module.exports = {
    TextInput: TextInput
}